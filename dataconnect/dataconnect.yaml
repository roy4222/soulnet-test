specVersion: "v1beta"
serviceId: "soulnet-test"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "soulnet-test-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
